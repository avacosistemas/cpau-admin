import { environment } from "environments/environment";

export const BANNER_CAMPAING_NAV_DEF = {
  id: 'publicidad',
  translateKey: 'banner_campaing_nav_def',
  url: '/' + environment.URL_PUBLICIDAD,
};
